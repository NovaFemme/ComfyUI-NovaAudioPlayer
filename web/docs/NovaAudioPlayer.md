# ComfyUI-NovaAudioPlayer

The maintained documentation is the `README.md` at the package root — this copy
was a snapshot and had already drifted (it still described five visualisers).

Keeping two copies means one of them is always wrong, so this file deliberately
holds no content of its own.

See:

* `README.md` — layout, how to add a view mode, colour roles, the settings
  drawer, renderer parameters, HTTP endpoints, local development.
* `web/renderers/_template.js` — the starting point for a new renderer.
